id: me.amankhanna.opendeck

runtime: org.gnome.Platform
runtime-version: "48"
sdk: org.gnome.Sdk

rename-icon: opendeck
rename-desktop-file: opendeck.desktop
rename-appdata-file: opendeck.metainfo.xml

command: opendeck
finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --device=all
  - --share=ipc
  - --share=network
  - --talk-name=org.freedesktop.Flatpak # Host command access is required to invoke Wine and Node, but only if the user has and wants to have them installed.
  - --talk-name=org.kde.StatusNotifierWatcher
  - --filesystem=xdg-run/tray-icon:create
  - --talk-name=org.opendeck.SingleInstance
  - --own-name=org.opendeck.SingleInstance
  - --talk-name=org.mpris.MediaPlayer2.*

modules:
  - shared-modules/libayatana-appindicator/libayatana-appindicator-gtk3.json
  - name: opendeck
    buildsystem: simple
    sources:
      - type: file
        url: https://github.com/nekename/OpenDeck/releases/download/v2.7.1/opendeck_2.7.1_amd64.deb
        sha256: 426ed8f42124595f48956fe70dd3e78747045173c6b3fb4c39774460960ac13c
        only-arches: [ x86_64 ]
      - type: file
        url: https://github.com/nekename/OpenDeck/releases/download/v2.7.1/opendeck_2.7.1_arm64.deb
        sha256: c96ce1ce0f34a630836b0cd0c07f99110dce47ea0132fb3ab8b142c995de9b1b
        only-arches: [ aarch64 ]
    build-commands:
      - ar -x *.deb
      - tar -xf data.tar.gz
      - install -Dm755 usr/bin/opendeck /app/bin/opendeck
      - install -Dm644 usr/share/icons/hicolor/512x512/apps/opendeck.png /app/share/icons/hicolor/512x512/apps/opendeck.png
      - install -Dm644 usr/share/applications/opendeck.desktop /app/share/applications/opendeck.desktop
      - install -Dm644 usr/share/metainfo/opendeck.metainfo.xml /app/share/metainfo/opendeck.metainfo.xml
      - cp -r usr/lib/opendeck/ /app/lib/opendeck/
